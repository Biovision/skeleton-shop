<% content_for :meta_title, @category.name %>

<article class="shop-category">
  <h1><%= @category.name %></h1>
  <%= render partial: 'shop/categories/list', locals: { categories: @category.subcategories } %>

  <%= simple_format @category.description %>
</article>
