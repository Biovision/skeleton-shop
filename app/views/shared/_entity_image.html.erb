<% unless entity.image.blank? %>
    <figure>
      <%= link_to image_tag(entity.image.preview.url, alt: t(:current_image)), entity.image.url %>
      <figcaption><%= t(:current_image) %></figcaption>
    </figure>
<% end %>