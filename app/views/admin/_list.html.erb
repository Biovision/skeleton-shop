<% if collection.any? %>
    <ul>
      <% collection.each do |entity| %>
      <li><%= link_to entity.name, entity %></li>
      <% end %>
    </ul>
<% else %>
    <div class="warning" title="<%= t('flash_messages.warning') %>"><%= t(:nothing_found) %></div>
<% end %>
