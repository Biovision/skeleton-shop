<% content_for :meta_title, t('.title', page: current_page) %>
<% content_for :options do %>
    <ul>
      <li><%= link_to t(:create), new_item_path %></li>
    </ul>
<% end %>

<article class="items">
  <h1><%= t('.header') %></h1>

  <%= paginate @collection %>
  <%= render partial: 'admin/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
